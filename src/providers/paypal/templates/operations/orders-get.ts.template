if (!params.id) {
        throw new Error('Order ID is required');
      }
      
      // Get an order through PayPal Checkout API
      const path = `/v2/checkout/orders/${params.id}`;
      
      // Execute the request
      const order = await paypal.get(path);
      
      return order;