// Format the path based on resource and ID
      const path = '${version}/${resource}/' + params.id;
      
      // Execute the request
      const result = await paypal.delete(path);
      
      return { 
        success: true, 
        id: params.id,
        ...result
      };